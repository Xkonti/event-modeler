# Event Modeler backend — local dev env
# During local dev, .env is assumed identical to this file.
POSTGRESQL_CONNECTION_STRING=postgresql://event_modeler:event_modeler@localhost:5432/event_modeler
PORT=3000
