## Repository Structure

Index of top-level dirs + purpose. Loads every session → orient fast. Expand this file as new dirs added.

## Directories

### `notes/`

AI-managed knowledge base on the project. Author thoughts, speculations, decisions → eventual basis for formal requirements + business logic. Agents write + organize. See scoped rule (loads when reading `notes/` files) for full management duties.
